<%= form_for(event_book) do |f| %>
  <% if event_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_book.errors.count, "error") %> prohibited this event_book from being saved:</h2>

      <ul>
      <% event_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
